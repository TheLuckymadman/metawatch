services:
  db:
    image: postgres:18
    # restart: always
    env_file:
      - .env
    volumes:
      - pgdata2:/var/lib/postgresql
    healthcheck:
      test: ["CMD-SHELL", "pg_isready"]
      interval: 1s
      timeout: 5s
      retries: 10
    ports:
    - "5432:5432"
volumes:
  pgdata2: